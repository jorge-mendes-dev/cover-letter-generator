[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  # Free tier max is 10s; Pro allows up to 26s.
  # PDF parsing + LLM generation can take 10–20s — increase if on Pro.
  timeout = 26
